# CrazyMarket Improvement Board

## Active

None.

## Paused

### Player Controller V2 vertical slice

- **Outcome:** The player can move, jump, and double-jump responsively in the Player V2 Test Campus scene, with animation matching the action on the frame it begins.
- **Why now:** The current controller work is already integrated and player-visible, but continued framework and timing refinement is delaying a shippable locomotion slice.
- **Current behavior:** A KCC-backed V2 controller, locomotion state machine, double-jump component, runtime profile, animation presenter, prefab, and dedicated Test Campus scene exist. Recent commits repeatedly refined jump/animation timing. A checkpoint smoke on 2026-08-18 confirmed the project compiles, the V2 scene enters Play Mode, and the Console has no errors; input feel and reset/boundary behavior were not re-exercised during this checkpoint.
- **Minimum:** Freeze architecture and ship the integrated walk/run, jump, double-jump, grounded reset, and immediate animation behavior already present. Make only fixes required by review or Unity smoke validation.
- **Target:** Confirm tuning is usable in Play Mode and that Test Campus reset/teleport/control-block paths preserve controller state.
- **Stretch:** None for this improvement.
- **Acceptance criteria:**
  - Movement is camera-relative and responsive in `TestCampus_Core_PlayerV2`.
  - Ground jump and one air jump both fire once per press and show the correct animation immediately.
  - Stable landing restores the air jump; walking off a ledge respects the intended coyote window.
  - Reset/teleport returns the player to a controllable, finite-velocity state.
  - Disabling and restoring movement does not retain queued jump input.
  - The scene compiles, enters Play Mode, and produces no errors or exceptions during the exercised paths.
- **Non-goals:** Additional locomotion modes, wall-kicks or ledge systems, more abilities, a general gameplay-state framework, further profile/modifier API expansion, new editor tooling, or production-scene replacement.
- **Scene/zone:** `Assets/TestCampus/Scenes/TestCampus_Core_PlayerV2.unity`.
- **Branch/base:** `main` / `origin/main`; local branch is 27 commits ahead.
- **Status:** Checkpointed; scope expansion paused pending user direction.
- **Budget:** Two implementation approaches and one review/fix round. Implementation budget is exhausted; retain the single review/fix round for Finish.
- **Attempts used:** Two prototypes (`player locomotion HSM`, `ledge jump recovery`) followed by the integrated V2 approach; approximately 15 controller-specific implementation/fix commits.
- **Progress:** Integrated V2 slice exists across 11 handwritten C# files (about 1,690 lines), a prefab, animator changes, and a dedicated scene. The work exceeds the steady-improvement tripwires for runtime components and handwritten lines.
- **Decisions:** Treat existing generality as sunk implementation for this slice, but add no more abstraction unless a concrete acceptance failure requires it. Do not fold the separate camera/URP working-tree changes into this improvement.
- **Validation:** On 2026-08-18, Unity 6000.4.1f1 reported scripts up to date; `TestCampus_Core_PlayerV2` was the clean active scene; Play Mode entered successfully; the error Console remained empty. No full interactive acceptance pass has been recorded yet.
- **Checkpoints:**
  - **2026-08-18 — depth/scope:** The work is too deep for one bounded improvement: it crossed the >3 runtime-component and ~1,000-line tripwires, consumed the implementation-attempt budget, and includes generalized profiles, modifiers, contracts, editor tooling, and ability composition beyond the player-visible minimum. Smallest shippable option: freeze the design and finish/validate the current movement-jump-double-jump slice. Larger option: continue developing a reusable controller platform, which requires a new improvement contract, explicit production requirements, and a fresh multi-round budget. Recommendation: take the smallest option and defer the platform work.

<!-- When active, keep: Outcome, Why now, Current behavior, Minimum, Target,
Stretch, Acceptance criteria, Non-goals, Scene/zone, Branch/base, Status,
Budget, Attempts used, Progress, Decisions, Validation, and Checkpoints. -->

## Next

<!-- At most three shaped candidates. -->

## Inbox

<!-- Capture adjacent discoveries without expanding Active. -->

- Decide whether runtime profile replacement and stat modifiers have a near-term production consumer; otherwise simplify or defer them after the V2 slice ships.
- Revisit wall/ledge recovery only as a separate player-visible improvement with its own Test Campus scenario.
- Reconcile the unrelated camera, renderer/URP, legacy KCC prefab, and Unity-upgrade working-tree changes separately from Player Controller V2.
- Exercise the Balanced and Performant URP assets under Unity 6.6 and disable their removed Dynamic Batching setting if those quality tiers emit the same Play Mode error.
- Dress the checkout/register foreground with existing-art belt detail, grocery bags or basket silhouettes, and a card-reader shape before sourcing a replacement model.
- Add restrained long-period camera drift only after the static framing is approved.
- Replace the two placeholder capsule shoppers with a Blobby-style shopper or cart-bearing silhouettes.
- Add a closing-time ambient bed: fluorescent hum, distant cart movement, and sparse PA texture.
- Evaluate subtle backdrop-only depth of field after framing; the UI Toolkit receipt itself renders outside the 3D post-process.
- Refine checkout warmth versus cool far-aisle panels and the checker-floor tone only if the new framing still competes with the receipt.
- Fix near-shelf item clipping and vary repeated shelf bays as a generator-owned environment cleanup.

## Completed

<!-- Newest first: date, outcome, validated scope, branch/commits, deferred work. -->

### 2026-09-05 - Subtle pickup VFX around the item, with the front clear

- **Outcome:** Following the user's correction to the bright first preview, the menu apple has a dim golden halo, at most three idle glints, and five brief collection glints. The effect sits behind and around the item, leaving its camera-facing surface readable. Idle glow contracts at collection; the grab burst fires once and clears on reset.
- **Scope:** Menu scene, existing `MainMenuBlobbyPickup`, and one procedural glint shader/material. Glow color (0.34, 0.20, 0.075), scale (0.65, 0.55, 0.35), and rear offset 1.6 preserve a soft edge while clearing the full glow footprint. Idle glints emit at 1.1/sec; collection glints last 0.25-0.4 seconds and originate 0.5 units behind the hand/item. Shared `Glow.vfx`, level pickups, Blobby's movement/arm animation, lighting, and receipt remain unchanged by this VFX pass.
- **Validation - verified:** Compiled successfully. Final saved-scene Main Menu Play ran for 69.7 seconds before Play Mode was stopped, covering two grabs/bursts, one completed lap/reset, and one checkout stop. Both 1920x1080 and 1280x800 idle/grab views were inspected. Sampled idle glints stayed at least 1.5955 units behind the apple center; collection glints stayed at least 0.4810 behind it. Counts never exceeded three/five. The preceding refinement run also covered two full laps; forced mid-burst cancellation, disable/re-enable, and missing optional burst reference passed before the final halo-depth adjustment. Shader supported with no errors/messages; final Console has zero errors and the pre-existing convex-hull warning only.
- **Evidence/handoff:** `Logs/menu-vfx-polish/` holds final observation, boundary and persistence reports plus idle/grab captures at both sizes. Saved scene matches the after-Play copy; five lightmaps and correct particle references persist. Original editor remains stopped and clean, original Game-view aspect restored, temporary size removed. One implementation approach and one focused refinement/review round. User requested all outstanding workspace changes be committed in multiple coherent commits on `stack/main-menu-closing-time` (base `e3144f2`); this entry accompanies the menu-scene commit. No standalone-build validation claimed.

### 2026-09-05 - Level-style pickup glow, right-hand stop, and translucent receipt

- **Outcome:** The menu apple uses the shared level `Glow.vfx` with the production Eggs pickup's golden color/rate, replacing the thin halo and floor rings. Blobby now picks it up at spline t=0.52 instead of 0.25, on the visible right-hand return stretch. The user's additional request makes main/settings receipt paper 84% opaque with lighter cast shading; ink and controls remain fully opaque, allowing Blobby to show through the menu.
- **Ownership:** Only the menu scene and existing `MainMenu.uss` changed. The scene adds four serialized objects/components and removes eight for the old rings; existing objects change only item position, glow reference, and pickup stop. Runtime scripts, spline shape, clips, shared VFX and level prefabs, lighting, and gameplay collection rules are unchanged. Paper fill and torn edge meet without overlap; no new runtime components or assets.
- **Validation - verified:** Unity compiled/up to date and the saved production Main Menu was exercised in Play Mode. An 85-second run covered two pickup stops, two checkout stops, and two laps. All 827 mesh samples stayed above the floor (minimum y=108.2712); paused speed and held item hand error were zero. Mid-grab cancellation, disable/re-enable, missing path/recovery, item/effect lap reset, and root reset passed. Root reset restarted 78 visible particles; no gameplay Item component is present. Reopened saved scene preserved t=0.52, the exact production VFX graph/color/rate, five lightmaps, and one Animator owner.
- **UI/visual checks:** Main, Options, Back, and UI activation reset passed at 1920x1080 and 1280x800. Resolved paper/edge opacity=0.84, parent/title opacity=1, seam gap=0, printer top=0, and paper fit all passed. Inspected pickup reach/held/stowed, both receipt pages, and Blobby naturally running behind the translucent receipt at both resolutions. Final Console has zero errors and only the pre-existing convex-hull warning.
- **Evidence/handoff:** `Logs/menu-pickup-right/` contains observation/boundary/persistence reports, `ui-validation.txt`, pickup phase screenshots, and `blobby-through-receipt-1080.png` / `blobby-through-receipt-800.png`. One implementation approach and one focused review round. Original editor remains responsive, stopped, and clean; after-Play scene matches disk, original aspect restored, temporary QA size removed. Local and uncommitted on `stack/main-menu-closing-time` at `e3144f2`; unrelated dirty work preserved. No standalone-build validation claimed.

### 2026-09-05 - Employee wayfinding and Blobby contact shadow

- **Outcome:** A cream-on-green "PICK. RUN. RETURN." banner, employee picking label, return-to-staging reminder, and raised suspended "01 / PRODUCE" marker reinforce the user's sole-employee grocery-run premise. A soft radial contact shadow keeps Blobby visually connected to the green floor through movement and stops.
- **Scope/ownership:** Added scene-owned signage and a quad child of the menu runner, three menu-only materials, and `Assets/Shaders/MainMenuContactShadow.shader`. Existing font and primitive meshes reused. No runtime C# added. Object-by-object comparison against the saved baseline has 54 added serialized objects/components, none removed, and only the new root/child references added to existing objects. Animation, lighting, five baked lightmaps, UI, and gameplay rules remain as authored.
- **Validation - verified:** Production Main Menu compiled/up to date. An 85-second Play run covered two aisle stops, two checkout stops, and two laps. All 826 samples kept the shadow centered and flat at y=108.265 (0.015 above the floor); no wall/shelf projection is used. Runner disable/re-enable and root activation reset passed. The shader is supported with no compiler messages/errors, and all five TMP labels fit. Reopened the saved scene and confirmed marker placement, suspension terminating at ceiling y=142.5, one Animator controller, and five lightmaps. Inspected final composited Game views at 1920x1080 and 1280x800; labels clear the shelf and receipt. Final Console: zero errors, only the pre-existing convex-hull warning.
- **Review/evidence:** One implementation approach and one focused review/fix round; raised the marker after its first placement was partly occluded by shelving. Evidence: `Logs/menu-wayfinding/final-1080.png`, `final-800.png`, `observation.tsv`, `observation-done.txt`, `boundaries.txt`, and `persistence.txt`. No temporary scripts under Assets; scene serializer whitespace is untouched.
- **Handoff:** Local, uncommitted on `stack/main-menu-closing-time` at `e3144f2`; unrelated dirty work preserved. Original editor retained; Game-view test size removed and original aspect restored. No standalone-build validation or further scope claimed.

### 2026-09-05 - Top-attached receipt and taller, warmer supermarket

- **Final floor clarification:** The user clarified that "brighter green" meant a deeper green. Final menu floor base color is (0.18, 0.38, 0.24), producing a richer forest-green checkerboard beneath the neutral lighting. Replaced the temporary pale-green trial, rebaked, and verified the final 1920x1080 production Game view with zero Console errors and only the existing convex-hull warning. Reloading the saved scene preserved the floor material, higher ceiling, and five lightmaps. Evidence: `Logs/menu-floor-brighter/deep-final-1080.png` and `persistence.txt`. Original editor remains stopped, responsive, and clean; temporary QA sizes are confirmed removed.
- **Outcome:** Receipt printer attaches to the top edge at all tested aspect ratios. Neutral white overhead light and warmer walls remove the overall green cast while retaining the user's green checkerboard floor. The ceiling, eleven fixtures, and frame are 14 world units higher, increasing headroom from 20.25 to 34.25; the user explicitly requested the second height increase. Lighting output was adjusted for the height and rebaked into five 1024 atlases.
- **Ownership:** Changed only the two existing UITK assets, four menu materials, scene lighting/ceiling transforms and baked lighting. The scene differs from the open-scene baseline by 48 replaced lines. The current menu-controller source, player, route and pickup implementation remain unchanged. Preserved the user's existing scene edits and a baseline copy; the added PanelRenderer remains configured but disabled, while the existing UIDocument is re-enabled to reconnect the visible receipt to its controller. Its previous disconnected tree had NaN bounds and could not drive the displayed receipt.
- **Validation - verified:** Unity 6000.6.0f1 reported scripts up to date. Reopened the saved production Main Menu and confirmed ceiling underside y=142.5, all fixtures below it, eleven overhead lights, five lightmaps, and one active UI owner. Production Play Mode passed main receipt, Options, Settings Back, and UI disable/re-enable paths using existing menu methods. At 1920x1080, 1280x800, and 1024x768, the printer top measured exactly zero, active paper stayed within the viewport, and transitions finished. Inspected composited Game views at all three resolutions and the Settings page. Final Play Mode Console had zero errors and the pre-existing `pb_Mesh-103204` convex-hull warning only.
- **Review/evidence:** One focused local review/fix round, with user-directed palette and height adjustments. No new runtime code or temporary scripts under Assets. Evidence: `Logs/menu-warm/final-main-1080.png`, `final-main-800.png`, `final-main-768.png`, `final-settings-1080.png`, `final-reset-768.png`, `ui-validation.txt`, and `persistence.txt`. A transient automation timeout during baking succeeded once the bake finished; this was cleared before the final Play Mode pass.
- **Handoff:** Original editor remains responsive, stopped, and clean. The after-Play scene copy matches the saved scene, original Game-view size is restored, and temporary QA sizes were removed. Local and uncommitted on `stack/main-menu-closing-time` at `e3144f2`; unrelated dirty work preserved. No standalone-build or end-to-end input-device validation claimed.

### 2026-09-04 - Separate checkout stop and aisle pickup

- **Outcome:** Blobby idles for 1.8 seconds beside the checkout conveyor and reaches for a glowing apple during his separate 2.6-second aisle stop. The apple follows his hand, is stowed, and returns for the next lap. The pickup is menu choreography and does not raise gameplay collection events.
- **Ownership:** Updated the menu runner, added `MainMenuBlobbyPickup`, one glow material, and the scene's apple/glow objects and references. The scene differs from this turn's saved baseline by 559 added and two replaced lines. The original 14-knot route, Idle/Running clips, single root Animator, lighting, and UI remain intact. The controller is byte-equivalent to this turn's starting controller.
- **Correction/review:** The first humanoid IK approach missed the visible hand on this imported rig. The second approach solves only the two arm bones and blends their rotations into/out of the animated pose. A fixed 0.04-world-unit menu visual clearance removes a small foot dip exposed by the new checkout transition. Two implementation approaches and one focused review/fix round used; no temporary scripts under Assets.
- **Validation - verified for this scope:** Unity 6000.6.0f1 compiled. A 100-second production Main Menu run covered three aisle pauses, two checkout pauses, and two laps. All 948 rendered-mesh samples remained above floor y=108.25 (minimum 108.2613); both stops held zero speed, the apple followed the hand exactly after grabbing, and visibility reset each lap. The final arm-easing refinement passed a separate 15-second aisle approach/reach/collection/restart run with 139 samples (minimum sole y=108.2926, maximum pre-grab error 0.0297, held error zero). Inspected composited 1920x1080 approach/reach/held/stowed and checkout Game views.
- **Boundaries/Console:** Disabling mid-grab restored both arm rotations, the authored player/item transforms, item scale, glow visibility, and cleared progress. Re-enable reset both stop flags and movement. Missing route held idle with the pickup reset; restoring the route resumed movement. Zero Console errors; only the pre-existing `pb_Mesh-103204` convex-hull warning occurred on scene load.
- **Persistence/evidence:** Reopened the saved production Main Menu and confirmed the item is visual-only and dynamic, both stop settings persist, five lightmaps remain, and exactly one Animator owns the original clips. Editor left stopped and clean; original Game-view size restored. Evidence: `Logs/menu-pickup/observation.tsv`, `reach-final.tsv`, `boundaries.txt`, `persistence.txt`, and phase screenshots.
- **Branch/status:** Local, uncommitted on `stack/main-menu-closing-time` at `e3144f2`; unrelated dirty work preserved. No gameplay inventory or standalone-build validation claimed; no further scope deferred from this request.

### 2026-09-04 - Brighter green store and corrected Blobby stopping

- **Outcome:** The Main Menu has brighter green fluorescent illumination, lighter walls/floor/ceiling, and a softer vignette. Blobby retains his original Running and Idle clips, with gentler acceleration/deceleration, fixed 0.18-second transitions, movement-only cadence, and a brief settling interval before the produce glance.
- **Ownership:** Only the menu runner, one menu-specific Animator controller, menu scene, four menu materials, menu volume profile, and its baked lighting were changed. The shared gameplay Animator and clips are unchanged. The scene differs from the saved turn-start snapshot by 49 replaced lines, including lighting, root-controller assignment, three tuning fields, and one insignificant quaternion serialization change. Five 1024 lightmap atlases remain.
- **User correction:** Removed the rejected mixed-stride walk and alternate idle clip. Restored the two nested Animators to their original null-controller state after the first authoring helper incorrectly assigned them controllers and introduced additional root motion. Exactly one original root Animator owns the rig in the saved scene.
- **Validation - verified for this scope:** Unity 6000.6.0f1 compiled successfully. An 80-second production Main Menu run covered approach, settling, glance, restart, and a full lap at speeds 0-7. All 751 sampled rendered-mesh minima remained above the floor at y=108.25 (lowest y=108.2511). Separately sampled both full clips at 31 phases. Inspected composited 1920x1080 Game views of all four transitions. Disable during the glance restored the authored transform, head poses, and Animator speed; re-enable reset speed/distance. Missing/open paths held position and idled; restoring the closed route resumed movement. The final observation and boundary pass had zero Console errors or warnings.
- **Persistence/evidence:** Reopened the saved Main Menu and confirmed one animation owner, the original Idle/Running clip references, five lightmaps, stopped Play Mode, and a clean scene. Original Game-view size restored. Evidence: `Logs/menu-brighter/verified-approach.png`, `verified-settle.png`, `verified-glance.png`, `verified-restart.png`, `observation-final.tsv`, and `boundaries.txt`. No temporary scripts were added under Assets.
- **Recovery:** After the initial inspection hung Unity, the user authorized reopening the original CrazyMarket project in one editor. That editor remains open and responsive; the discarded recovery instance is stopped.
- **Branch/status:** Local and uncommitted on `stack/main-menu-closing-time` at `e3144f2`; unrelated dirty work preserved. Two bounded animation approaches and one focused correction/review round used. No standalone-build or shared gameplay-controller validation claimed.


### 2026-09-04 - Blobby menu movement and produce glance

- **Outcome:** Blobby accelerates into a gentler jog, anticipates corners, slows through tight bends, and pauses for 2.6 seconds once per lap to glance at produce. Animation cadence follows movement; the head turn eases in and out on both existing rigs.
- **Ownership:** Changed only the menu runner and its scene configuration, preserving the authored route, UI, environment polish, and shared gameplay Animator assets. The scene diff against the saved starting snapshot contains only 13 added and two replaced runner-configuration lines. One implementation approach and one focused local review were used.
- **Validation - verified for this scope:** Unity 6000.6.0f1 compiled successfully. A 78-second observation in the production Main Menu completed two laps and two pauses, with movement between 0 and 7 units/second and tight-corner speed reaching 2.8. Inspected 1920x1080 Game-view captures at the produce pause and a corner. Missing/open path probes held position and idled; disabling during the glance restored both head poses, authored position, and Animator speed; re-enabling reset distance/speed and resumed movement. No new Console errors occurred; existing plugin-importer and ProBuilder convex-hull warnings remain.
- **Evidence:** `Temp/menu-review/blobby-observation.json`, `blobby-boundaries.json`, `blobby-pause.png`, and `blobby-corner.png`. Unity is stopped, the scene is saved, and the original Game-view setting is restored.
- **Branch/status:** Local, uncommitted on `stack/main-menu-closing-time` at `e3144f2`; unrelated dirty work preserved. NPCs, contact shadows, and standalone-build/Steam certification remain outside this movement pass.

### 2026-09-04 ? Main Menu environment polish and shelf seating

- **Outcome:** The saved closing-time Main Menu has visible ceiling practicals, quieter teal floor and walls, a readable produce sign, and correctly seated shelf products. The receipt UI is unchanged in this pass.
- **Ownership:** The user explicitly approved saving the pre-existing scene edits (Blobby route and camera far-distance adjustment), then explicitly included menu-only product variants and shelf regeneration. Those changes were preserved as the baseline. Work remains local and uncommitted on `stack/main-menu-closing-time` (`e3144f2`).
- **Changes:** Persisted the 10-degree camera pitch; seated all eleven ceiling fixtures below the lowered ceiling and placed their Area Lights below the housings; tuned the menu-only floor and both wall material slots; added a restrained world-space sign using the existing static LiberationSans atlas; deactivated the two capsule placeholder shoppers. Kept the authored Blobby route and gameplay item assets.
- **Shelf ownership:** Nine menu-only visual stock prefabs normalize product bottoms and horizontal centers. Carrots are reduced to fit the shelf depth; cereal uses a saved copy of its ProBuilder mesh. The scene-owned ShelfItemSpawner lists and 114 SplineInstantiate inputs reference these variants. No production prefab or shared generator code was changed. Reauthor these visual copies deliberately if their source models change.
- **Generated verification:** 646 products across 114 rows. A second clear/regenerate produced identical bounds and positions. Every product bottom is 0.0116?0.0126 world units above its board; no product intersects the shelf above or the ceiling. The shelf update changed 1,596/1,444 serialized lines relative to the immediately preceding scene snapshot, primarily prefab references and row transforms. No stale baked roots were found.
- **Lighting:** Final 8-texel bake has five directional 1024 atlases. Transient metadata write errors occurred at bake completion; all ten lightmap textures then saved and reimported successfully, with no subsequent errors. The saved scene was reopened to check persistence.
- **Validation ? verified for this scope:** Unity 6000.6.0f1 reports scripts up to date. The production scene was exercised in Play Mode, stopped, reopened from disk, and re-entered. Inspected actual 1280x800, 1920x1080, and 2560x1080 Game views. Blobby continues along the route; menu navigation skips Leaderboard (`0?1?3`), Options opens, and Settings Back returns to the menu. These interaction probes used the existing view methods, not an end-to-end input-device test. The loaded scene has zero missing scripts. Final reload/Play Mode had zero new errors; existing ProBuilder convex-hull and six old plugin-importer warnings remain.
- **Evidence:** `Temp/menu-review/complete-800.png`, `complete-1080.png`, and `complete-ultrawide.png`; the initial view is `polish-before.png`. Evidence is outside Assets. Unity is left stopped with the Main Menu saved and the original Game-view setting restored. No temporary Editor scripts remain in Assets.
- **Limits/deferred:** This verifies the environment polish, not a bug-free Steam release. No standalone build, Steam integration, performance certification, or full input-device matrix was tested. Existing checkout-model detail and optional Blobby contact shadow remain separate polish candidates.

### 2026-09-04 — Main Menu floor-shadow cleanup

- **Outcome:** Removed the distracting aisle-spanning projected shadow bands from the closing-time Main Menu while preserving the lit ceiling, oversized store composition, receipt focus, and moving Blobby.
- **Validated scope:** Kept Fable's focused material/light correction: the menu-only checker floor no longer takes realtime light-color contribution, the realtime directional remains a shadowless cool fill, and the two checkout spots retain their local lighting without painting large moving bands across the floor. Restored the intended compact 4-texel lighting setting after the unfinished 8-texel diagnostic bake and regenerated only the production Main Menu lighting data.
- **Behavior evidence:** Unity 6000.6.0f1 ran the production `Main Menu` scene with a visually inspected composited 1280x800 Game view; the floor remained clean across Blobby's loop and both receipt pages. Production navigation skipped the disabled Leaderboard (`0→1→3`), opened Options, traversed Settings to Back (`0→1→2→3`), and returned to the main receipt focused on Options while Blobby continued moving. The final isolated runtime Console segment contained zero errors and zero warnings; the earlier scene-load-only `pb_Mesh-103204` convex-hull warning remains pre-existing.
- **Lighting evidence:** The final Progressive GPU bake completed in 17.5 seconds at 4 texels/unit with three directional 1024 atlases and a 12 MB reported lightmap payload, replacing the unfinished five-atlas 8-texel experiment.
- **Validation note:** The package's generic runtime input injector flushes synthetic events before this polling view's `Update`, so this follow-up exercised the unchanged production navigation/submit methods directly. The 2026-09-03 real Input System pass remains the input-specific evidence.
- **Branch/commits:** `stack/main-menu-closing-time`, based on `e3144f2`; local changes remain uncommitted and unrelated Unity-upgrade work remains untouched.
- **Deferred work:** A dedicated stylized contact shadow for Blobby is optional future polish, not part of this artifact-removal fix.

### 2026-09-03 — Oversized after-hours Main Menu ambience

- **Outcome:** Made the checkout Main Menu feel like an oversized supermarket after closing, with a structured lit ceiling, substantial shelf walls, larger products on the near shelf, and animated Blobby movement behind the receipt.
- **Validated scope:** Preserved the user-authored 1.5x Blobby scale; set both shelf-line roots to 5x; lifted the lower right shelf so its rendered model clears the finished floor; enlarged and reseated the products in the right shelf bays by 1.5x; added a dark ceiling grid, emissive practical panels, eight baked cool Area Lights, cool directional fill, and warm realtime checkout accents. Added a serialized six-knot closed `SplineContainer` and a focused `MainMenuBlobbyRunner` that moves, turns, and drives the existing Running state without changing production player-controller ownership.
- **Behavior evidence:** Unity 6000.6.0f1 compiled after the temporary authoring helper was removed. In the production `Main Menu` scene, the lower-right shelf model measured `108.358` at its lowest point against a floor top of `108.250`. Blobby reset to `(72, 108.3, -245)`, entered the existing Running state, advanced to `(56.56, 108.3, -226.66)` on the smooth loop, and restored its authored transform after leaving Play Mode. Options opened through real Input System keyboard events and Escape returned to the main receipt while Blobby continued moving. Composited Game views were visually inspected at 1280x800 and 1920x1080. The final Play Mode Console had zero errors; the one `pb_Mesh-103204` convex-hull warning predates this pass.
- **Lighting evidence:** The final Progressive GPU bake completed in 12.6 seconds at 4 texels/unit with two directional 1024 atlases and an 8 MB reported lightmap payload. The first high-density attempt produced 71 atlases and was rejected and removed before the compact rebake.
- **Review note:** The review-loop `unity-smoke` command is hard-coded for Test Campus validation, teleport, and fixture reset probes, so the required production-scene compile, Play Mode, lifecycle reset, interaction, Console, bounds, lighting-bake, and visual checks supplied the applicable feature evidence.
- **Branch/commits:** `stack/main-menu-closing-time`, based on `e3144f2`; local changes remain uncommitted and unrelated Unity-upgrade work remains untouched.
- **Deferred work:** Additional store signage remains optional. The pre-existing ProBuilder convex-hull warning should be handled separately if that source mesh becomes gameplay-relevant.

### 2026-09-03 — Closing-time checkout Main Menu

- **Outcome:** Reframed the Main Menu as a closing-time checkout-to-aisle scene where the receipt remains the focal point and Blobby reads as a small character inside the larger store.
- **Validated scope:** Moved the camera into the checkout aisle; reduced Blobby from 10x to a visually tuned 3.5x; moved two 6x NPC walkers into the far third; added a ceiling, menu-only desaturated floor, warm checkout spot lights, cool linear fog, a menu-only bloom profile, and SMAA; folded best-run information into the receipt footer and removed the detached card. Disabled the removed Dynamic Batching option on the active High Fidelity URP asset so Unity 6.6 Play Mode starts cleanly.
- **Behavior evidence:** Unity 6000.6.0f1 recompiled with zero errors and ran the production `Main Menu` scene with an empty error Console. Blobby's idle bones advanced and both NPC spline walkers changed position. Input System navigation wrapped `Continue → Quit → Continue`, skipped the disabled Leaderboard, opened Options, and Escape returned to the main receipt with Options focused. Final Game views were visually inspected at 1280×720 and 1280×800; the 2.5x scale study hid too much of Blobby behind the counter, while 3.5x retained the peek and restored the intended player-to-environment relationship.
- **Review note:** The generic `unity-smoke` command is built around Test Campus validation and teleport/reset probes, so focused production-scene compile, interaction, motion, Console, and visual checks supplied the applicable acceptance evidence.
- **Branch/commits:** `stack/main-menu-closing-time`, based on `e3144f2`; local changes remain uncommitted.
- **Deferred work:** Hanging aisle signs or additional store branding remain optional stretch work.

### 2026-08-27 — Accessible main-menu keyboard navigation

- **Outcome:** Discrete Up/Down keyboard presses now move exactly one enabled Main Menu option at a time without requiring Left/Right or held inputs.
- **Validated scope:** Made `MainMenuToolkitView` the single owner of directional navigation by suppressing UI Toolkit's duplicate automatic `NavigationMoveEvent`; kept pointer behavior, gamepad/manual input, wraparound, Settings controls, and the disabled Leaderboard unchanged. No scene, UXML, font, package, recovery, or serialized asset was changed for this fix.
- **Behavior evidence:** The production combined-path repro failed before the fix on the first Down press (`Continue → Options`, index `0→3`) and passed after the fix. The final Play Mode sequence traversed Down `0→1→3→4→0` and Up `0→4→3→1→0`, repeated both cycles, skipped Leaderboard deliberately, opened Settings, returned to Options, and moved to Quit on the next Down press. Main and Settings Game views were visually inspected at the active 16:9 aspect; the isolated Console showed zero logs, warnings, or errors. Unity returned to Edit Mode with the pre-existing dirty scene state preserved.
- **Root cause:** Each physical direction was handled once by `MainMenuToolkitView.ReadInput()` and a second time by UI Toolkit's automatic focus navigation.
- **Branch/commits:** `main`; completion commit based on `b5f4ac0`.
- **Deferred work:** None for this defect.

### 2026-08-25 — Receipt-printer detail and Settings receipt

- **Outcome:** Made the production UI Toolkit register feel like a physical receipt printer, gave the unavailable Leaderboard row a naturally rubbed-off treatment, and made Options tear off the main receipt before printing a fully interactive Settings receipt.
- **Validated scope:** Added a recessed slot, serrated tear comb, rollers, vents, shell seam, fasteners, model plate, contact shadow, feed-state light, paper edge definition, a licensed CC0 rubbed-print overlay, a shorter Settings slip, stepped sensitivity/volume scales, an animated centered invert check, Back/Escape/gamepad-B behavior, and overlapping tear/feed choreography. Removed the redundant receipt-side Best Run while preserving the detached Best Run · All Levels ticket. Kept the Post-It variant archived and left the 3D environment untouched.
- **Behavior evidence:** Unity 6000.4.1f1 compiled with zero errors. Both receipt states were visually inspected at 1024×768, 1280×720, 1280×800, 1920×800, and 1920×1080. Real Options submission kept the main canvas active and legacy Settings canvas inactive. Settings navigation wrapped `0→3→0`; main navigation returned to Options and wrapped `3→4→0`; Leaderboard stayed disabled and unfocusable. Sensitivity, volume, and invert updated through the existing settings owner and were restored after testing. Back reprinted Main with focus on Options. Both NPC walkers moved and the active KCC animator advanced. The isolated Play Mode Console had zero errors; one pre-existing convex-mesh warning was unrelated.
- **Design review:** A dedicated design pass refined the printer hardware, overlapping paper motion, shortened Settings slip, header collision fix, contact shadow, typography floor, control alignment, and stronger printed-paper treatment.
- **Branch/commits:** `main`; atomic UI Toolkit receipt commit based on `a81b571`.
- **Deferred work:** Leaderboard still has no backend by design. A dedicated user-facing reduced-motion preference can later drive the already-supported reduced-motion transition path.

### 2026-08-24 — Main Menu UI Toolkit migration

- **Outcome:** Migrated the approved sticky-note Main Menu from generated uGUI to one runtime `UIDocument`, then corrected the Toolkit authoring defaults and composition to match the approved web and GameObject versions without changing the live market backdrop or menu action ownership.
- **Validated scope:** Removed `Sticky Menu Visuals`, `StickyMainMenuView`, and `StickyMenuRow`; made the complete UXML composition visible by default in UI Builder; restored the approved 1280×800 design frame, 470×400 note, 47px row rhythm, Atma typography, copy, logo colors, and tinted interaction art; removed the UI-owned vignette so post-processing owns screen-edge treatment; grouped the logo into independently movable `market-word` and `dash-word` authoring units with an explicit gap; joined the marker arrow shaft and right-facing head into one slim continuous visual; preserved entrance staging, hover/selection styling, submit feedback, keyboard/gamepad wrap navigation, save-driven best-run state, idle timer, leaderboard unavailable feedback, and the unchanged legacy Settings canvas.
- **Behavior evidence:** The production scene compiled and ran in Unity 6000.4.1f1. The corrected Play Mode view was visually inspected against `Prototypes/MainMenu/final-sticky.html`; Blobby's active animator advanced and both NPC spline walkers changed world position. The selection arrow resolved to a 180-degree right-facing head, a roughly 4.3px shaft, and approximately 5.2px of shaft/head overlap, then followed the selected row through `Quit -> Continue` wrap navigation. Keyboard and gamepad navigation wrapped selection; Options opened Settings and returned to a rebuilt UI Toolkit tree with one callback bound. The isolated final Play Mode Console contained zero errors.
- **Review note:** The generic `unity-smoke` baseline expected the seven-scene Test Campus topology and therefore rejected the production menu's single loaded scene. Focused production Play Mode, interaction, lifecycle, motion, visual, and Console checks supplied the applicable acceptance evidence.
- **Branch/commits:** `main`; local uncommitted migration based on `a81b571`.
- **Deferred work:** Settings remains on its existing uGUI canvas by request. Leaderboards remains a serialized placeholder/no-op until a backend exists.

### 2026-08-23 — Approved sticky-note Main Menu

- **Outcome:** Replaced the production Main Menu visuals with the approved sticky-note composition while preserving the existing settings canvas, fade transition, controller API, and save ownership.
- **Validated scope:** Live gameplay backdrop with Blobby's idle/breathing animation and both NPC paths preserved; vignette; stacked Market Dash logo; outlined/curling sticky note; five menu rows; shared hover/selection arrow and background dim; green submit check and squash; idle timer reset; save-driven best-run card and empty state; leaderboard no-op shake; Options open/close reset; keyboard and gamepad wrap navigation; entrance staging; 1280x800, 1920x1080, and 1920x800 visual layouts.
- **Behavior evidence:** The production scene compiled and ran in Unity 6000.4.1f1. Mouse hover/submit and Settings return were exercised in the Game view. Connected Input System events moved `Continue -> New Game`, wrapped `Continue -> Quit` by keyboard, and wrapped `Quit -> Continue` by gamepad. A 12-second production capture confirmed the live Blobby animation and both NPC paths continued behind the UI. The isolated final Play Mode log contained zero relevant errors, exceptions, or assertions.
- **Review note:** The generic `unity-smoke` baseline was run but expected the seven-scene Test Campus topology and found the one loaded production menu scene. Focused production Play Mode and visual evidence supplied the applicable acceptance signal.
- **Branch/commits:** `main`; atomic feature commit prepared for publication.
- **Deferred work:** New Game still intentionally has no confirmation dialog, and Leaderboards remains a serialized placeholder/no-op until a backend exists.
